sudo: false
language: node_js
node_js:
  - "node"

before_script:
  - npm install
  - npm install -g coveralls

after_success:
  - npm run coveralls

deploy:
  provider: heroku
  api_key:
    secure: ${env:api_key}
  on:
    all_branches: true
  app: chatbot-condominio
